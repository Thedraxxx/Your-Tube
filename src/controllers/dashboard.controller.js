import { asyncHandler } from "../utils/asyncHandeler.js";

const getChannlStatus = asyncHandler(async(req,res)=>
{

});
const getChannelVideos = asyncHandler(async(req,res)=>{

})

export {getChannlStatus, getChannelVideos}